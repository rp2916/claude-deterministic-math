---
name: Deterministic Math Engine
description: Solves complex mathematical equations with 100% precision by executing them in Python rather than predicting them.
---

# Instructions

1. When the user asks a complex math question, translate their request into a valid Python math expression (e.g., `math.sqrt(144) + math.pow(12, 2)`).
2. Pass ONLY that translated math expression as a string argument to `calculator.py`.
3. Display the exact output returned by the script.
